{
  "name": "argus",
  "version": "1.0.0",
  "description": "Command-line interface for the Coop supermarket chain",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cheerio": "^0.22.0",
    "lodash": "^4.16.6",
    "request": "^2.78.0"
  }
}
